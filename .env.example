# The plugin no longer reads any VITE_* env var — credentials are entered at
# runtime via the in-WebView config form on first launch and persisted via
# the Even Hub bridge (with browser localStorage as a fallback for the
# desktop simulator).
#
# This file is kept as a placeholder; nothing here is consumed by the build.
